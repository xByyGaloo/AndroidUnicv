import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'unicvlanches',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
